global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ['localhost:9090']

  - job_name: cadvisor
    static_configs:
      - targets: ['cadvisor:8080']

  - job_name: traefik
    static_configs:
      - targets: ['traefik:8080']
    metrics_path: /metrics

  # Stub jobs — uncomment when the services are added
  # - job_name: grafana
  #   static_configs:
  #     - targets: ['grafana:3000']
  #   metrics_path: /metrics

  # - job_name: minio
  #   static_configs:
  #     - targets: ['minio:9000']
  #   metrics_path: /minio/v2/metrics/cluster
