# Copied into projects/<name>/.env by `make new-project name=<name>` (you fill in real values).
# Names only — no real values committed.

# Hostname this service answers on: <name>.tarik-lab.dev
PROJECT_NAME=demo

# Version string returned by GET / — bump per release.
APP_VERSION=0.1.0
