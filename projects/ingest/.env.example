# Copied into projects/ingest/.env — fill in real values, never commit .env.
# Names only — no real values committed.

PROJECT_NAME=ingest
APP_VERSION=0.1.0

# S3/MinIO — use the scoped key from task 6.2, not root creds
AWS_ENDPOINT_URL=https://minio.tarik-lab.dev
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
S3_BUCKET=demo-data
